/**
 * @license
 * Copyright (c) 2016, Contributors
 * SPDX-License-Identifier: ISC
 */
import { tokenizeArgString } from './tokenize-arg-string.js';
import { DefaultValuesForTypeKey } from './yargs-parser-types.js';
import { camelCase, decamelize, looksLikeNumber } from './string-utils.js';
let mixin;
export class YargsParser {
    constructor(_mixin) {
        mixin = _mixin;
    }
    parse(argsInput, options) {
        const opts = Object.assign({
            alias: undefined,
            array: undefined,
            boolean: undefined,
            config: undefined,
            configObjects: undefined,
            configuration: undefined,
            coerce: undefined,
            count: undefined,
            default: undefined,
            envPrefix: undefined,
            narg: undefined,
            normalize: undefined,
            string: undefined,
            number: undefined,
            __: undefined,
            key: undefined
        }, options);
        // allow a string argument to be passed in rather
        // than an argv array.
        const args = tokenizeArgString(argsInput);
        // tokenizeArgString adds extra quotes to args if argsInput is a string
        // only strip those extra quotes in processValue if argsInput is a string
        const inputIsString = typeof argsInput === 'string';
        // aliases might have transitive relationships, normalize this.
        const aliases = combineAliases(Object.assign(Object.create(null), opts.alias));
        const configuration = Object.assign({
            'boolean-negation': true,
            'camel-case-expansion': true,
            'combine-arrays': false,
            'dot-notation': true,
            'duplicate-arguments-array': true,
            'flatten-duplicate-arrays': true,
            'greedy-arrays': true,
            'halt-at-non-option': false,
            'nargs-eats-options': false,
            'negation-prefix': 'no-',
            'parse-numbers': true,
            'parse-positional-numbers': true,
            'populate--': false,
            'set-placeholder-key': false,
            'short-option-groups': true,
            'strip-aliased': false,
            'strip-dashed': false,
            'unknown-options-as-args': false
        }, opts.configuration);
        const defaults = Object.assign(Object.create(null), opts.default);
        const configObjects = opts.configObjects || [];
        const envPrefix = opts.envPrefix;
        const notFlagsOption = configuration['populate--'];
        const notFlagsArgv = notFlagsOption ? '--' : '_';
        const newAliases = Object.create(null);
        const defaulted = Object.create(null);
        // allow a i18n handler to be passed in, default to a fake one (util.format).
        const __ = opts.__ || mixin.format;
        const flags = {
            aliases: Object.create(null),
            arrays: Object.create(null),
            bools: Object.create(null),
            strings: Object.create(null),
            numbers: Object.create(null),
            counts: Object.create(null),
            normalize: Object.create(null),
            configs: Object.create(null),
            nargs: Object.create(null),
            coercions: Object.create(null),
            keys: []
        };
        const negative = /^-([0-9]+(\.[0-9]+)?|\.[0-9]+)$/;
        const negatedBoolean = new RegExp('^--' + configuration['negation-prefix'] + '(.+)');
        [].concat(opts.array || []).filter(Boolean).forEach(function (opt) {
            const key = typeof opt === 'object' ? opt.key : opt;
            // assign to flags[bools|strings|numbers]
            const assignment = Object.keys(opt).map(function (key) {
                const arrayFlagKeys = {
                    boolean: 'bools',
                    string: 'strings',
                    number: 'numbers'
                };
                return arrayFlagKeys[key];
            }).filter(Boolean).pop();
            // assign key to be coerced
            if (assignment) {
                flags[assignment][key] = true;
            }
            flags.arrays[key] = true;
            flags.keys.push(key);
        });
        [].concat(opts.boolean || []).filter(Boolean).forEach(function (key) {
            flags.bools[key] = true;
            flags.keys.push(key);
        });
        [].concat(opts.string || []).filter(Boolean).forEach(function (key) {
            flags.strings[key] = true;
            flags.keys.push(key);
        });
        [].concat(opts.number || []).filter(Boolean).forEach(function (key) {
            flags.numbers[key] = true;
            flags.keys.push(key);
        });
        [].concat(opts.count || []).filter(Boolean).forEach(function (key) {
            flags.counts[key] = true;
            flags.keys.push(key);
        });
        [].concat(opts.normalize || []).filter(Boolean).forEach(function (key) {
            flags.normalize[key] = true;
            flags.keys.push(key);
        });
        if (typeof opts.narg === 'object') {
            Object.entries(opts.narg).forEach(([key, value]) => {
                if (typeof value === 'number') {
                    flags.nargs[key] = value;
                    flags.keys.push(key);
                }
            });
        }
        if (typeof opts.coerce === 'object') {
            Object.entries(opts.coerce).forEach(([key, value]) => {
                if (typeof value === 'function') {
                    flags.coercions[key] = value;
                    flags.keys.push(key);
                }
            });
        }
        if (typeof opts.config !== 'undefined') {
            if (Array.isArray(opts.config) || typeof opts.config === 'string') {
                ;
                [].concat(opts.config).filter(Boolean).forEach(function (key) {
                    flags.configs[key] = true;
                });
            }
            else if (typeof opts.config === 'object') {
                Object.entries(opts.config).forEach(([key, value]) => {
                    if (typeof value === 'boolean' || typeof value === 'function') {
                        flags.configs[key] = value;
                    }
                });
            }
        }
        // create a lookup table that takes into account all
        // combinations of aliases: {f: ['foo'], foo: ['f']}
        extendAliases(opts.key, aliases, opts.default, flags.arrays);
        // apply default values to all aliases.
        Object.keys(defaults).forEach(function (key) {
            (flags.aliases[key] || []).forEach(function (alias) {
                defaults[alias] = defaults[key];
            });
        });
        let error = null;
        checkConfiguration();
        let notFlags = [];
        const argv = Object.assign(Object.create(null), { _: [] });
        // TODO(bcoe): for the first pass at removing object prototype  we didn't
        // remove all prototypes from objects returned by this API, we might want
        // to gradually move towards doing so.
        const argvReturn = {};
        for (let i = 0; i < args.length; i++) {
            const arg = args[i];
            const truncatedArg = arg.replace(/^-{3,}/, '---');
            let broken;
            let key;
            let letters;
            let m;
            let next;
            let value;
            // any unknown option (except for end-of-options, "--")
            if (arg !== '--' && /^-/.test(arg) && isUnknownOptionAsArg(arg)) {
                pushPositional(arg);
                // ---, ---=, ----, etc,
            }
            else if (truncatedArg.match(/^---+(=|$)/)) {
                // options without key name are invalid.
                pushPositional(arg);
                continue;
                // -- separated by =
            }
            else if (arg.match(/^--.+=/) || (!configuration['short-option-groups'] && arg.match(/^-.+=/))) {
                // Using [\s\S] instead of . because js doesn't support the
                // 'dotall' regex modifier. See:
                // http://stackoverflow.com/a/1068308/13216
                m = arg.match(/^--?([^=]+)=([\s\S]*)$/);
                // arrays format = '--f=a b c'
                if (m !== null && Array.isArray(m) && m.length >= 3) {
                    if (checkAllAliases(m[1], flags.arrays)) {
                        i = eatArray(i, m[1], args, m[2]);
                    }
                    else if (checkAllAliases(m[1], flags.nargs) !== false) {
                        // nargs format = '--f=monkey washing cat'
                        i = eatNargs(i, m[1], args, m[2]);
                    }
                    else {
                        setArg(m[1], m[2], true);
                    }
                }
            }
            else if (arg.match(negatedBoolean) && configuration['boolean-negation']) {
                m = arg.match(negatedBoolean);
                if (m !== null && Array.isArray(m) && m.length >= 2) {
                    key = m[1];
                    setArg(key, checkAllAliases(key, flags.arrays) ? [false] : false);
                }
                // -- separated by space.
            }
            else if (arg.match(/^--.+/) || (!configuration['short-option-groups'] && arg.match(/^-[^-]+/))) {
                m = arg.match(/^--?(.+)/);
                if (m !== null && Array.isArray(m) && m.length >= 2) {
                    key = m[1];
                    if (checkAllAliases(key, flags.arrays)) {
                        // array format = '--foo a b c'
                        i = eatArray(i, key, args);
                    }
                    else if (checkAllAliases(key, flags.nargs) !== false) {
                        // nargs format = '--foo a b c'
                        // should be truthy even if: flags.nargs[key] === 0
                        i = eatNargs(i, key, args);
                    }
                    else {
                        next = args[i + 1];
                        if (next !== undefined && (!next.match(/^-/) ||
                            next.match(negative)) &&
                            !checkAllAliases(key, flags.bools) &&
                            !checkAllAliases(key, flags.counts)) {
                            setArg(key, next);
                            i++;
                        }
                        else if (/^(true|false)$/.test(next)) {
                            setArg(key, next);
                            i++;
                        }
                        else {
                            setArg(key, defaultValue(key));
                        }
                    }
                }
                // dot-notation flag separated by '='.
            }
            else if (arg.match(/^-.\..+=/)) {
                m = arg.match(/^-([^=]+)=([\s\S]*)$/);
                if (m !== null && Array.isArray(m) && m.length >= 3) {
                    setArg(m[1], m[2]);
                }
                // dot-notation flag separated by space.
            }
            else if (arg.match(/^-.\..+/) && !arg.match(negative)) {
                next = args[i + 1];
                m = arg.match(/^-(.\..+)/);
                if (m !== null && Array.isArray(m) && m.length >= 2) {
                    key = m[1];
                    if (next !== undefined && !next.match(/^-/) &&
                        !checkAllAliases(key, flags.bools) &&
                        !checkAllAliases(key, flags.counts)) {
                        setArg(key, next);
                        i++;
                    }
                    else {
                        setArg(key, defaultValue(key));
                    }
                }
            }
            else if (arg.match(/^-[^-]+/) && !arg.match(negative)) {
                letters = arg.slice(1, -1).split('');
                broken = false;
                for (let j = 0; j < letters.length; j++) {
                    next = arg.slice(j + 2);
                    if (letters[j + 1] && letters[j + 1] === '=') {
                        value = arg.slice(j + 3);
                        key = letters[j];
                        if (checkAllAliases(key, flags.arrays)) {
                            // array format = '-f=a b c'
                            i = eatArray(i, key, args, value);
                        }
                        else if (checkAllAliases(key, flags.nargs) !== false) {
                            // nargs format = '-f=monkey washing cat'
                            i = eatNargs(i, key, args, value);
                        }
                        else {
                            setArg(key, value);
                        }
                        broken = true;
                        break;
                    }
                    if (next === '-') {
                        setArg(letters[j], next);
                        continue;
                    }
                    // current letter is an alphabetic character and next value is a number
                    if (/[A-Za-z]/.test(letters[j]) &&
                        /^-?\d+(\.\d*)?(e-?\d+)?$/.test(next) &&
                        checkAllAliases(next, flags.bools) === false) {
                        setArg(letters[j], next);
                        broken = true;
                        break;
                    }
                    if (letters[j + 1] && letters[j + 1].match(/\W/)) {
                        setArg(letters[j], next);
                        broken = true;
                        break;
                    }
                    else {
                        setArg(letters[j], defaultValue(letters[j]));
                    }
                }
                key = arg.slice(-1)[0];
                if (!broken && key !== '-') {
                    if (checkAllAliases(key, flags.arrays)) {
                        // array format = '-f a b c'
                        i = eatArray(i, key, args);
                    }
                    else if (checkAllAliases(key, flags.nargs) !== false) {
                        // nargs format = '-f a b c'
                        // should be truthy even if: flags.nargs[key] === 0
                        i = eatNargs(i, key, args);
                    }
                    else {
                        next = args[i + 1];
                        if (next !== undefined && (!/^(-|--)[^-]/.test(next) ||
                            next.match(negative)) &&
                            !checkAllAliases(key, flags.bools) &&
                            !checkAllAliases(key, flags.counts)) {
                            setArg(key, next);
                            i++;
                        }
                        else if (/^(true|false)$/.test(next)) {
                            setArg(key, next);
                            i++;
                        }
                        else {
                            setArg(key, defaultValue(key));
                        }
                    }
                }
            }
            else if (arg.match(/^-[0-9]$/) &&
                arg.match(negative) &&
                checkAllAliases(arg.slice(1), flags.bools)) {
                // single-digit boolean alias, e.g: xargs -0
                key = arg.slice(1);
                setArg(key, defaultValue(key));
            }
            else if (arg === '--') {
                notFlags = args.slice(i + 1);
                break;
            }
            else if (configuration['halt-at-non-option']) {
                notFlags = args.slice(i);
                break;
            }
            else {
                pushPositional(arg);
            }
        }
        // order of precedence:
        // 1. command line arg
        // 2. value from env var
        // 3. value from config file
        // 4. value from config objects
        // 5. configured default value
        applyEnvVars(argv, true); // special case: check env vars that point to config file
        applyEnvVars(argv, false);
        setConfig(argv);
        setConfigObjects();
        applyDefaultsAndAliases(argv, flags.aliases, defaults, true);
        applyCoercions(argv);
        if (configuration['set-placeholder-key'])
            setPlaceholderKeys(argv);
        // for any counts either not in args or without an explicit default, set to 0
        Object.keys(flags.counts).forEach(function (key) {
            if (!hasKey(argv, key.split('.')))
                setArg(key, 0);
        });
        // '--' defaults to undefined.
        if (notFlagsOption && notFlags.length)
            argv[notFlagsArgv] = [];
        notFlags.forEach(function (key) {
            argv[notFlagsArgv].push(key);
        });
        if (configuration['camel-case-expansion'] && configuration['strip-dashed']) {
            Object.keys(argv).filter(key => key !== '--' && key.includes('-')).forEach(key => {
                delete argv[key];
            });
        }
        if (configuration['strip-aliased']) {
            ;
            [].concat(...Object.keys(aliases).map(k => aliases[k])).forEach(alias => {
                if (configuration['camel-case-expansion'] && alias.includes('-')) {
                    delete argv[alias.split('.').map(prop => camelCase(prop)).join('.')];
                }
                delete argv[alias];
            });
        }
        // Push argument into positional array, applying numeric coercion:
        function pushPositional(arg) {
            const maybeCoercedNumber = maybeCoerceNumber('_', arg);
            if (typeof maybeCoercedNumber === 'string' || typeof maybeCoercedNumber === 'number') {
                argv._.push(maybeCoercedNumber);
            }
        }
        // how many arguments should we consume, based
        // on the nargs option?
        function eatNargs(i, key, args, argAfterEqualSign) {
            let ii;
            let toEat = checkAllAliases(key, flags.nargs);
            // NaN has a special meaning for the array type, indicating that one or
            // more values are expected.
            toEat = typeof toEat !== 'number' || isNaN(toEat) ? 1 : toEat;
            if (toEat === 0) {
                if (!isUndefined(argAfterEqualSign)) {
                    error = Error(__('Argument unexpected for: %s', key));
                }
                setArg(key, defaultValue(key));
                return i;
            }
            let available = isUndefined(argAfterEqualSign) ? 0 : 1;
            if (configuration['nargs-eats-options']) {
                // classic behavior, yargs eats positional and dash arguments.
                if (args.length - (i + 1) + available < toEat) {
                    error = Error(__('Not enough arguments following: %s', key));
                }
                available = toEat;
            }
            else {
                // nargs will not consume flag arguments, e.g., -abc, --foo,
                // and terminates when one is observed.
                for (ii = i + 1; ii < args.length; ii++) {
                    if (!args[ii].match(/^-[^0-9]/) || args[ii].match(negative) || isUnknownOptionAsArg(args[ii]))
                        available++;
                    else
                        break;
                }
                if (available < toEat)
                    error = Error(__('Not enough arguments following: %s', key));
            }
            let consumed = Math.min(available, toEat);
            if (!isUndefined(argAfterEqualSign) && consumed > 0) {
                setArg(key, argAfterEqualSign);
                consumed--;
            }
            for (ii = i + 1; ii < (consumed + i + 1); ii++) {
                setArg(key, args[ii]);
            }
            return (i + consumed);
        }
        // if an option is an array, eat all non-hyphenated arguments
        // following it... YUM!
        // e.g., --foo apple banana cat becomes ["apple", "banana", "cat"]
        function eatArray(i, key, args, argAfterEqualSign) {
            let argsToSet = [];
            let next = argAfterEqualSign || args[i + 1];
            // If both array and nargs are configured, enforce the nargs count:
            const nargsCount = checkAllAliases(key, flags.nargs);
            if (checkAllAliases(key, flags.bools) && !(/^(true|false)$/.test(next))) {
                argsToSet.push(true);
            }
            else if (isUndefined(next) ||
                (isUndefined(argAfterEqualSign) && /^-/.test(next) && !negative.test(next) && !isUnknownOptionAsArg(next))) {
                // for keys without value ==> argsToSet remains an empty []
                // set user default value, if available
                if (defaults[key] !== undefined) {
                    const defVal = defaults[key];
                    argsToSet = Array.isArray(defVal) ? defVal : [defVal];
                }
            }
            else {
                // value in --option=value is eaten as is
                if (!isUndefined(argAfterEqualSign)) {
                    argsToSet.push(processValue(key, argAfterEqualSign, true));
                }
                for (let ii = i + 1; ii < args.length; ii++) {
                    if ((!configuration['greedy-arrays'] && argsToSet.length > 0) ||
                        (nargsCount && typeof nargsCount === 'number' && argsToSet.length >= nargsCount))
                        break;
                    next = args[ii];
                    if (/^-/.test(next) && !negative.test(next) && !isUnknownOptionAsArg(next))
                        break;
                    i = ii;
                    argsToSet.push(processValue(key, next, inputIsString));
                }
            }
            // If both array and nargs are configured, create an error if less than
            // nargs positionals were found. NaN has special meaning, indicating
            // that at least one value is required (more are okay).
            if (typeof nargsCount === 'number' && ((nargsCount && argsToSet.length < nargsCount) ||
                (isNaN(nargsCount) && argsToSet.length === 0))) {
                error = Error(__('Not enough arguments following: %s', key));
            }
            setArg(key, argsToSet);
            return i;
        }
        function setArg(key, val, shouldStripQuotes = inputIsString) {
            if (/-/.test(key) && configuration['camel-case-expansion']) {
                const alias = key.split('.').map(function (prop) {
                    return camelCase(prop);
                }).join('.');
                addNewAlias(key, alias);
            }
            const value = processValue(key, val, shouldStripQuotes);
            const splitKey = key.split('.');
            setKey(argv, splitKey, value);
            // handle populating aliases of the full key
            if (flags.aliases[key]) {
                flags.aliases[key].forEach(function (x) {
                    const keyProperties = x.split('.');
                    setKey(argv, keyProperties, value);
                });
            }
            // handle populating aliases of the first element of the dot-notation key
            if (splitKey.length > 1 && configuration['dot-notation']) {
                ;
                (flags.aliases[splitKey[0]] || []).forEach(function (x) {
                    let keyProperties = x.split('.');
                    // expand alias with nested objects in key
                    const a = [].concat(splitKey);
                    a.shift(); // nuke the old key.
                    keyProperties = keyProperties.concat(a);
                    // populate alias only if is not already an alias of the full key
                    // (already populated above)
                    if (!(flags.aliases[key] || []).includes(keyProperties.join('.'))) {
                        setKey(argv, keyProperties, value);
                    }
                });
            }
            // Set normalize getter and setter when key is in 'normalize' but isn't an array
            if (checkAllAliases(key, flags.normalize) && !checkAllAliases(key, flags.arrays)) {
                const keys = [key].concat(flags.aliases[key] || []);
                keys.forEach(function (key) {
                    Object.defineProperty(argvReturn, key, {
                        enumerable: true,
                        get() {
                            return val;
                        },
                        set(value) {
                            val = typeof value === 'string' ? mixin.normalize(value) : value;
                        }
                    });
                });
            }
        }
        function addNewAlias(key, alias) {
            if (!(flags.aliases[key] && flags.aliases[key].length)) {
                flags.aliases[key] = [alias];
                newAliases[alias] = true;
            }
            if (!(flags.aliases[alias] && flags.aliases[alias].length)) {
                addNewAlias(alias, key);
            }
        }
        function processValue(key, val, shouldStripQuotes) {
            // strings may be quoted, clean this up as we assign values.
            if (shouldStripQuotes) {
                val = stripQuotes(val);
            }
            // handle parsing boolean arguments --foo=true --bar false.
            if (checkAllAliases(key, flags.bools) || checkAllAliases(key, flags.counts)) {
                if (typeof val === 'string')
                    val = val === 'true';
            }
            let value = Array.isArray(val)
                ? val.map(function (v) { return maybeCoerceNumber(key, v); })
                : maybeCoerceNumber(key, val);
            // increment a count given as arg (either no value or value parsed as boolean)
            if (checkAllAliases(key, flags.counts) && (isUndefined(value) || typeof value === 'boolean')) {
                value = increment();
            }
            // Set normalized value when key is in 'normalize' and in 'arrays'
            if (checkAllAliases(key, flags.normalize) && checkAllAliases(key, flags.arrays)) {
                if (Array.isArray(val))
                    value = val.map((val) => { return mixin.normalize(val); });
                else
                    value = mixin.normalize(val);
            }
            return value;
        }
        function maybeCoerceNumber(key, value) {
            if (!configuration['parse-positional-numbers'] && key === '_')
                return value;
            if (!checkAllAliases(key, flags.strings) && !checkAllAliases(key, flags.bools) && !Array.isArray(value)) {
                const shouldCoerceNumber = looksLikeNumber(value) && configuration['parse-numbers'] && (Number.isSafeInteger(Math.floor(parseFloat(`${value}`))));
                if (shouldCoerceNumber || (!isUndefined(value) && checkAllAliases(key, flags.numbers))) {
                    value = Number(value);
                }
            }
            return value;
        }
        // set args from config.json file, this should be
        // applied last so that defaults can be applied.
        function setConfig(argv) {
            const configLookup = Object.create(null);
            // expand defaults/aliases, in-case any happen to reference
            // the config.json file.
            applyDefaultsAndAliases(configLookup, flags.aliases, defaults);
            Object.keys(flags.configs).forEach(function (configKey) {
                const configPath = argv[configKey] || configLookup[configKey];
                if (configPath) {
                    try {
                        let config = null;
                        const resolvedConfigPath = mixin.resolve(mixin.cwd(), configPath);
                        const resolveConfig = flags.configs[configKey];
                        if (typeof resolveConfig === 'function') {
                            try {
                                config = resolveConfig(resolvedConfigPath);
                            }
                            catch (e) {
                                config = e;
                            }
                            if (config instanceof Error) {
                                error = config;
                                return;
                            }
                        }
                        else {
                            config = mixin.require(resolvedConfigPath);
                        }
                        setConfigObject(config);
                    }
                    catch (ex) {
                        // Deno will receive a PermissionDenied error if an attempt is
                        // made to l://github.com/dart-lang/http","description":"A composable, multi-platform, Future-based API for HTTP requests.","environment":{"sdk":">=2.12.0 <3.0.0"},"dependencies":{"async":"^2.5.0","http_parser":"^4.0.0","meta":"^1.3.0","path":"^1.8.0","pedantic":"^1.10.0"},"dev_dependencies":{"fake_async":"^1.2.0","shelf":"^1.1.0","test":"^1.16.0"}},"archive_url":"https://pub.dev/api/archives/http-0.13.2.tar.gz","archive_sha256":"0c51a50c5ac87b08e5e98a12efbd24bb8367268021de0a13b99319275c15eb4a","published":"2021-04-28T16:37:14.116276Z"},{"version":"0.13.3","pubspec":{"name":"http","version":"0.13.3","homepage":"https://github.com/dart-lang/http","description":"A composable, multi-platform, Future-based API for HTTP requests.","environment":{"sdk":">=2.12.0 <3.0.0"},"dependencies":{"async":"^2.5.0","http_parser":"^4.0.0","meta":"^1.3.0","path":"^1.8.0","pedantic":"^1.10.0"},"dev_dependencies":{"fake_async":"^1.2.0","shelf":"^1.1.0","test":"^1.16.0"}},"archive_url":"https://pub.dev/api/archives/http-0.13.3.tar.gz","archive_sha256":"b6f1f143a71e1fe1b34670f1acd6f13960ade2557c96b87e127e0cf661969791","published":"2021-05-03T19:14:58.986938Z"},{"version":"0.13.4","pubspec":{"name":"http","version":"0.13.4","homepage":"https://github.com/dart-lang/http","description":"A composable, multi-platform, Future-based API for HTTP requests.","environment":{"sdk":">=2.14.0 <3.0.0"},"dependencies":{"async":"^2.5.0","http_parser":"^4.0.0","meta":"^1.3.0","path":"^1.8.0"},"dev_dependencies":{"fake_async":"^1.2.0","lints":"^1.0.0","shelf":"^1.1.0","test":"^1.16.0"}},"archive_url":"https://pub.dev/api/archives/http-0.13.4.tar.gz","archive_sha256":"2ed163531e071c2c6b7c659635112f24cb64ecbebf6af46b550d536c0b1aa112","published":"2021-10-04T13:31:21.823064Z"},{"version":"0.13.5","pubspec":{"name":"http","version":"0.13.5","description":"A composable, multi-platform, Future-based API for HTTP requests.","repository":"https://github.com/dart-lang/http/tree/master/pkgs/http","environment":{"sdk":">=2.14.0 <3.0.0"},"dependencies":{"async":"^2.5.0","http_parser":"^4.0.0","meta":"^1.3.0","path":"^1.8.0"},"dev_dependencies":{"fake_async":"^1.2.0","http_client_conformance_tests":{"path":"../http_client_conformance_tests/"},"lints":"^1.0.0","shelf":"^1.1.0","stream_channel":"^2.1.0","test":"^1.16.0"}},"archive_url":"https://pub.dev/api/archives/http-0.13.5.tar.gz","archive_sha256":"6aa2946395183537c8b880962d935877325d6a09a2867c3970c05c0fed6ac482","published":"2022-08-03T20:06:06.554830Z"},{"version":"0.13.6","pubspec":{"name":"http","version":"0.13.6","description":"A composable, multi-platform, Future-based API for HTTP requests.","repository":"https://github.com/dart-lang/http/tree/master/pkgs/http","environment":{"sdk":">=2.19.0 <3.0.0"},"dependencies":{"async":"^2.5.0","http_parser":"^4.0.0","meta":"^1.3.0"},"dev_dependencies":{"dart_flutter_team_lints":"^1.0.0","fake_async":"^1.2.0","http_client_conformance_tests":{"path":"../http_client_conformance_tests/"},"shelf":"^1.1.0","stream_channel":"^2.1.0","test":"^1.16.0"}},"archive_url":"https://pub.dev/api/archives/http-0.13.6.tar.gz","archive_sha256":"5895291c13fa8a3bd82e76d5627f69e0d85ca6a30dcac95c4ea19a5d555879c2","published":"2023-05-01T17:54:17.086948Z"},{"version":"1.0.0","pubspec":{"name":"http","version":"1.0.0","description":"A composable, multi-platform, Future-based API for HTTP requests.","repository":"https://github.com/dart-lang/http/tree/master/pkgs/http","environment":{"sdk":"^3.0.0"},"dependencies":{"async":"^2.5.0","http_parser":"^4.0.0","meta":"^1.3.0"},"dev_dependencies":{"dart_flutter_team_lints":"^1.0.0","fake_async":"^1.2.0","http_client_conformance_tests":{"path":"../http_client_conformance_tests/"},"shelf":"^1.1.0","stream_channel":"^2.1.0","test":"^1.16.0"}},"archive_url":"https://pub.dev/api/archives/http-1.0.0.tar.gz","archive_sha256":"4c3f04bfb64d3efd508d06b41b825542f08122d30bda4933fb95c069d22a4fa3","published":"2023-05-22T18:23:29.020741Z"},{"version":"1.1.0","pubspec":{"name":"http","version":"1.1.0","description":"A composable, multi-platform, Future-based API for HTTP requests.","repository":"https://github.com/dart-lang/http/tree/master/pkgs/http","environment":{"sdk":"^3.0.0"},"dependencies":{"async":"^2.5.0","http_parser":"^4.0.0","meta":"^1.3.0"},"dev_dependencies":{"dart_flutter_team_lints":"^1.0.0","fake_async":"^1.2.0","http_client_conformance_tests":{"path":"../http_client_conformance_tests/"},"shelf":"^1.1.0","stream_channel":"^2.1.0","test":"^1.16.0"}},"archive_url":"https://pub.dev/api/archives/http-1.1.0.tar.gz","archive_sha256":"759d1a329847dd0f39226c688d3e06a6b8679668e350e2891a6474f8b4bb8525","published":"2023-06-26T21:35:21.113941Z"},{"version":"1.1.1","pubspec":{"name":"http","version":"1.1.1","description":"A composable, multi-platform, Future-based API for HTTP requests.","repository":"https://github.com/dart-lang/http/tree/master/pkgs/http","environment":{"sdk":"^3.2.0"},"dependencies":{"async":"^2.5.0","http_parser":"^4.0.0","meta":"^1.3.0","web":"^0.4.0"},"dev_dependencies":{"dart_flutter_team_lints":"^1.0.0","fake_async":"^1.2.0","http_client_conformance_tests":{"path":"../http_client_conformance_tests/"},"shelf":"^1.1.0","stream_channel":"^2.1.0","test":"^1.16.0"}},"archive_url":"https://pub.dev/api/archives/http-1.1.1.tar.gz","archive_sha256":"38dce67ce909c338754840e2a1479c2c11347a2fc7ec3d5b166a5118a8a201db","published":"2023-11-21T22:57:18.111534Z"},{"version":"1.1.2","pubspec":{"name":"http","version":"1.1.2","description":"A composable, multi-platform, Future-based API for HTTP requests.","repository":"https://github.com/dart-lang/http/tree/master/pkgs/http","environment":{"sdk":"^3.2.0"},"dependencies":{"async":"^2.5.0","http_parser":"^4.0.0","meta":"^1.3.0","web":">=0.3.0 <0.5.0"},"dev_dependencies":{"dart_flutter_team_lints":"^2.0.0","fake_async":"^1.2.0","http_client_conformance_tests":{"path":"../http_client_conformance_tests/"},"shelf":"^1.1.0","stream_channel":"^2.1.0","test":"^1.16.0"}},"archive_url":"https://pub.dev/api/archives/http-1.1.2.tar.gz","archive_sha256":"d4872660c46d929f6b8a9ef4e7a7eff7e49bbf0c4ec3f385ee32df5119175139","published":"2023-11-27T17:26:58.457130Z"},{"version":"1.2.0","pubspec":{"name":"http","version":"1.2.0","description":"A composable, multi-platform, Future-based API for HTTP requests.","repository":"https://github.com/dart-lang/http/tree/master/pkgs/http","environment":{"sdk":"^3.2.0"},"dependencies":{"async":"^2.5.0","http_parser":"^4.0.0","meta":"^1.3.0","web":">=0.3.0 <0.5.0"},"dev_dependencies":{"dart_flutter_team_lints":"^2.0.0","fake_async":"^1.2.0","http_client_conformance_tests":{"path":"../http_client_conformance_tests/"},"shelf":"^1.1.0","stream_channel":"^2.1.0","test":"^1.16.0"}},"archive_url":"https://pub.dev/api/archives/http-1.2.0.tar.gz","archive_sha256":"a2bbf9d017fcced29139daa8ed2bba4ece450ab222871df93ca9eec6f80c34ba","published":"2024-01-16T22:46:38.816321Z"},{"version":"1.2.1","pubspec":{"name":"http","version":"1.2.1","description":"A composable, multi-platform, Future-based API for HTTP requests.","repository":"https://github.com/dart-lang/http/tree/master/pkgs/http","environment":{"sdk":"^3.3.0"},"dependencies":{"async":"^2.5.0","http_parser":"^4.0.0","meta":"^1.3.0","web":"^0.5.0"},"dev_dependencies":{"dart_flutter_team_lints":"^2.0.0","fake_async":"^1.2.0","http_client_conformance_tests":{"path":"../http_client_conformance_tests/"},"shelf":"^1.1.0","stream_channel":"^2.1.0","test":"^1.16.0"}},"archive_url":"https://pub.dev/api/archives/http-1.2.1.tar.gz","archive_sha256":"761a297c042deedc1ffbb156d6e2af13886bb305c2a343a4d972504cd67dd938","published":"2024-02-15T23:25:27.572746Z"},{"version":"1.2.2","pubspec":{"name":"http","version":"1.2.2","description":"A composable, multi-platform, Future-based API for HTTP requests.","repository":"https://github.com/dart-lang/http/tree/master/pkgs/http","topics":["http","network","protocols"],"environment":{"sdk":"^3.3.0"},"dependencies":{"async":"^2.5.0","http_parser":"^4.0.0","meta":"^1.3.0","web":">=0.5.0 <2.0.0"},"dev_dependencies":{"dart_flutter_team_lints":"^3.0.0","fake_async":"^1.2.0","http_client_conformance_tests":{"path":"../http_client_conformance_tests/"},"shelf":"^1.1.0","stream_channel":"^2.1.1","test":"^1.21.2"}},"archive_url":"https://pub.dev/api/archives/http-1.2.2.tar.gz","archive_sha256":"b9c29a161230ee03d3ccf545097fccd9b87a5264228c5d348202e0f0c28f9010","published":"2024-07-16T20:09:47.863320Z"},{"version":"1.3.0","pubspec":{"name":"http","version":"1.3.0","description":"A composable, multi-platform, Future-based API for HTTP requests.","repository":"https://github.com/dart-lang/http/tree/master/pkgs/http","topics":["http","network","protocols"],"environment":{"sdk":"^3.4.0"},"dependencies":{"async":"^2.5.0","http_parser":"^4.0.0","meta":"^1.3.0","web":">=0.5.0 <2.0.0"},"dev_dependencies":{"dart_flutter_team_lints":"^3.0.0","fake_async":"^1.2.0","http_client_conformance_tests":{"path":"../http_client_conformance_tests/"},"shelf":"^1.1.0","stream_channel":"^2.1.1","test":"^1.21.2"}},"archive_url":"https://pub.dev/api/archives/http-1.3.0.tar.gz","archive_sha256":"fe7ab022b76f3034adc518fb6ea04a82387620e19977665ea18d30a1cf43442f","published":"2025-01-21T00:32:28.622865Z"},{"version":"1.4.0","pubspec":{"name":"http","version":"1.4.0","description":"A composable, multi-platform, Future-based API for HTTP requests.","repository":"https://github.com/dart-lang/http/tree/master/pkgs/http","topics":["http","network","protocols"],"environment":{"sdk":"^3.4.0"},"dependencies":{"async":"^2.5.0","http_parser":"^4.0.0","meta":"^1.3.0","web":">=0.5.0 <2.0.0"},"dev_dependencies":{"dart_flutter_team_lints":"^3.0.0","fake_async":"^1.2.0","http_client_conformance_tests":{"path":"../http_client_conformance_tests/"},"shelf":"^1.1.0","stream_channel":"^2.1.1","test":"^1.21.2"}},"archive_url":"https://pub.dev/api/archives/http-1.4.0.tar.gz","archive_sha256":"2c11f3f94c687ee9bad77c171151672986360b2b001d109814ee7140b2cf261b","published":"2025-05-06T00:17:13.636460Z"},{"version":"1.5.0-beta","pubspec":{"name":"http","version":"1.5.0-beta","description":"A composable, multi-platform, Future-based API for HTTP requests.","repository":"https://github.com/dart-lang/http/tree/master/pkgs/http","topics":["http","network","protocols"],"environment":{"sdk":"^3.4.0"},"dependencies":{"async":"^2.5.0","http_parser":"^4.0.0","meta":"^1.3.0","web":">=0.5.0 <2.0.0"},"dev_dependencies":{"dart_flutter_team_lints":"^3.0.0","fake_async":"^1.2.0","http_client_conformance_tests":{"path":"../http_client_conformance_tests/"},"shelf":"^1.1.0","stream_channel":"^2.1.1","test":"^1.21.2"}},"archive_url":"https://pub.dev/api/archives/http-1.5.0-beta.tar.gz","archive_sha256":"559ab0d950643c9a04512f4c4bd1c435dcb5af1aa9c848f3bf5867347044328a","published":"2025-07-08T17:55:50.858454Z"},{"version":"1.5.0-beta.2","pubspec":{"name":"http","version":"1.5.0-beta.2","description":"A composable, multi-platform, Future-based API for HTTP requests.","repository":"https://github.com/dart-lang/http/tree/master/pkgs/http","topics":["http","network","protocols"],"environment":{"sdk":"^3.4.0"},"dependencies":{"async":"^2.5.0","http_parser":"^4.0.0","meta":"^1.3.0","web":">=0.5.0 <2.0.0"},"dev_dependencies":{"dart_flutter_team_lints":"^3.0.0","fake_async":"^1.2.0","http_client_conformance_tests":{"path":"../http_client_conformance_tests/"},"shelf":"^1.1.0","stream_channel":"^2.1.1","test":"^1.21.2"}},"archive_url":"https://pub.dev/api/archives/http-1.5.0-beta.2.tar.gz","archive_sha256":"85ab0074f9bf2b24625906d8382bbec84d3d6919d285ba9c106b07b65791fb99","published":"2025-07-15T23:37:56.942822Z"},{"version":"1.5.0","pubspec":{"name":"http","version":"1.5.0","description":"A composable, multi-platform, Future-based API for HTTP requests.","repository":"https://github.com/dart-lang/http/tree/master/pkgs/http","topics":["http","network","protocols"],"environment":{"sdk":"^3.4.0"},"dependencies":{"async":"^2.5.0","http_parser":"^4.0.0","meta":"^1.3.0","web":">=0.5.0 <2.0.0"},"dev_dependencies":{"dart_flutter_team_lints":"^3.0.0","fake_async":"^1.2.0","http_client_conformance_tests":{"path":"../http_client_conformance_tests/"},"shelf":"^1.1.0","stream_channel":"^2.1.1","test":"^1.21.2"}},"archive_url":"https://pub.dev/api/archives/http-1.5.0.tar.gz","archive_sha256":"bb2ce4590bc2667c96f318d68cac1b5a7987ec819351d32b1c987239a815e007","published":"2025-08-07T22:35:23.863279Z"},{"version":"1.6.0","pubspec":{"name":"http","version":"1.6.0","description":"A composable, multi-platform, Future-based API for HTTP requests.","repository":"https://github.com/dart-lang/http/tree/master/pkgs/http","topics":["http","network","protocols"],"environment":{"sdk":"^3.4.0"},"dependencies":{"async":"^2.5.0","http_parser":"^4.0.0","meta":"^1.3.0","web":">=0.5.0 <2.0.0"},"dev_dependencies":{"dart_flutter_team_lints":"^3.0.0","fake_async":"^1.2.0","http_client_conformance_tests":{"path":"../http_client_conformance_tests/"},"shelf":"^1.1.0","stream_channel":"^2.1.1","test":"^1.21.2"}},"archive_url":"https://pub.dev/api/archives/http-1.6.0.tar.gz","archive_sha256":"87721a4a50b19c7f1d49001e51409bddc46303966ce89a65af4f4e6004896412","published":"2025-11-10T18:27:56.434747Z"}],"advisoriesUpdated":"2026-05-04T16:03:47.124153Z","_fetchedAt":"2026-08-14T08:33:27.184328"}                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  {"name":"shelf_web_socket","latest":{"version":"3.0.0","pubspec":{"name":"shelf_web_socket","version":"3.0.0","description":"A shelf handler that wires up a listener for every connection.","repository":"https://github.com/dart-lang/shelf/tree/master/pkgs/shelf_web_socket","issue_tracker":"https://github.com/dart-lang/shelf/issues?q=is%3Aissue+is%3Aopen+label%3Apackage%3Ashelf_web_socket","topics":["server","shelf"],"environment":{"sdk":"^3.5.0"},"dependencies":{"shelf":"^1.1.0","stream_channel":"^2.1.0","web_socket_channel":">=2.0.0 <4.0.0"},"dev_dependencies":{"dart_flutter_team_lints":"^3.0.0","http":">=0.13.0 <2.0.0","test":"^1.25.2"},"dependency_overrides":{"test":"^1.25.11"}},"archive_url":"https://pub.dev/api/archives/shelf_web_socket-3.0.0.tar.gz","archive_sha256":"3632775c8e90d6c9712f883e633716432a27758216dfb61bd86a8321c0580925","published":"2025-01-30T17:50:10.363347Z"},"versions":[{"version":"0.0.1","pubspec":{"description":"A WebSocket handler for Shelf.\n","author":"Dart Team <misc@dartlang.org>","environment":{"sdk":">=1.4.0-dev.5.0 <2.0.0"},"version":"0.0.1","dependencies":{"shelf":">=0.5.2 <0.6.0","http_parser":">=0.0.2 <0.1.0"},"dev_dependencies":{"unittest":">=0.10.0 <0.12.0","http":">=0.10.0 <0.12.0"},"homepage":"http://www.dartlang.org","name":"shelf_web_socket"},"archive_url":"https://pub.dev/api/archives/shelf_web_socket-0.0.1.tar.gz","archive_sha256":"58ff7d45a196f39e8c2d1bdce1282850c3e25acf2c0f0ea345f036f706455f32","published":"2014-05-20T22:17:56.367510Z"},{"version":"0.0.1+1","pubspec":{"description":"A WebSocket handler for Shelf.\n","author":"Dart Team <misc@dartlang.org>","environment":{"sdk":">=1.4.0-dev.5.0 <2.0.0"},"version":"0.0.1+1","dependencies":{"shelf":">=0.5.2 <0.6.0","http_parser":">=0.0.2 <0.1.0"},"dev